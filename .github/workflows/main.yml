name: Live SSH Access

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Start tmate session (SSH enabled)
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
          detached: false
